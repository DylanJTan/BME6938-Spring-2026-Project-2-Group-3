# Dependency management is handled via the conda environment named `pt`.
# This repository uses the existing conda env `pt` (already created on the compute
# nodes). To reproduce or export the environment, use the commands below.

# Export the full environment to a YAML file (recommended for sharing):
#   conda env export -n pt > environment.yml

# Create the environment on another machine from the exported YAML:
#   conda env create -f environment.yml -n pt

# If you need a pip-style requirements file for a narrower subset, generate it
# from the activated environment (optional):
#   conda activate pt
#   pip freeze > pip-requirements.txt

# NOTE: Do not run `pip install -r requirements.txt` — use the `pt` conda env.
# Install the skinlesions package itself in editable mode:
#   pip install -e .

# For documentation, minimal package set required by the skinlesions package:
numpy>=1.23
pandas>=1.5
scikit-learn>=1.2
scipy>=1.10
matplotlib>=3.6
seaborn>=0.12
Pillow>=9.0
tqdm>=4.64
PyYAML>=6.0
torch>=2.0
torchvision>=0.15
torchaudio>=2.0
jupyter>=1.0
ipykernel>=6.0
opencv-python>=4.7
grad-cam>=1.4
